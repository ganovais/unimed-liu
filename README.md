## Starting database application with docker
`docker-compose up -d`

### Starting backend
`npm install`
`npx prisma migrate deploy`
`npx prisma generate`
`npm run start:dev`

### Starting frontend
`npm install`
`npm run dev`

